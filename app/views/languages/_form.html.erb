<%= form_for(@language) do |f| %>
  <% if @language.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@language.errors.count, "error") %> prohibited this language from being saved:</h2>

      <ul>
      <% @language.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :langname, "Language Name" %><br>
    <%= f.text_field :langname, 
                     autofocus: true, 
                     required: true,
                     placeholder: "Example: English" %>
  </div>
  <div class="field">
    <%= f.label :langcode, "Language Code" %><br>
    <%= f.text_field :langcode, 
                     required: true, 
                     placeholder: "Example: en" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
